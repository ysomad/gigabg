services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: gigabg
      POSTGRES_PASSWORD: gigabg
      POSTGRES_DB: gigabg
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
